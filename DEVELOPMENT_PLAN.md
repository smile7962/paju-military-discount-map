# 파주시 군 장병 할인업소 지도 웹앱 — 개발 계획서

> V-World 지도 기반 / 모바일 우선 반응형 웹앱
> 데이터 출처: `★2026년도 군 장병 할인업소 현황(2026.3.1.기준).xlsx`

---

## 1. 목표

파주시 군 장병(및 사회복무요원) 할인업소 189개소를 스마트폰에서 쉽게 찾아볼 수 있는 지도 웹앱을 만든다.

- **권역(지역) 선택 → 업종 카테고리 선택** 2단계 필터로 원하는 업소를 빠르게 탐색
- V-World 지도 위에 마커 표시, 업소 상세(할인 내역, 전화 걸기) 제공
- 모바일 우선(Mobile-first)으로 설계하되 PC에서도 자연스러운 **반응형 UI**

## 2. 데이터 분석 결과

엑셀 파일 구조 (시트 1개, 헤더 2행 + 데이터 189행):

| 컬럼 | 내용 | 비고 |
|---|---|---|
| 연번 | 1~189 | ID로 사용 |
| 지역 | 12개 권역 | 광탄 9 · 교하 4 · 금촌 21 · 문산 38 · 법원 37 · 운정 11 · 월롱 1 · 적성 23 · 조리 2 · 탄현 26 · 파주(읍) 10 · 파평 7 |
| 업종 | 6종 | 일반음식점 140 · 미용업 24 · 숙박업 15 · 목욕장업 5 · 휴게음식점 4 · 복합(일반+휴게) 1 |
| 업소명 / 대표자 / 전화번호 | 상세 정보 | 전화번호로 `tel:` 링크 제공 |
| 주소 | 도로명 주소 | **좌표 없음 → 지오코딩 필요** |
| 할인현황 / 할인세부내역 | "10% 할인" 등 4종 | 마커 팝업·상세에 표시 |
| 사회복무요원 | ○ 표시 74개소 | 필터 토글로 제공 |

### 데이터 처리 방침

- **빌드 타임 변환**: 엑셀 → `data/stores.json` 변환 스크립트(Python) 작성. 런타임에 엑셀을 읽지 않음.
- **지오코딩은 1회 선처리**: V-World Geocoder API(`api.vworld.kr/req/address`)로 주소 → 경위도 좌표를 변환해 JSON에 포함. 실패 건은 로그로 남기고 수동 보정.
- **업종 정규화**: "일반음식점,휴게음식점" 복합 1건은 두 카테고리 모두에 노출. 사용자 노출용 카테고리명은 친숙하게 매핑(일반음식점→음식점, 휴게음식점→카페·간식, 미용업→미용, 목욕장업→목욕, 숙박업→숙박).
- **갱신 절차**: 시청에서 새 엑셀 배포 시 파일 교체 → 변환 스크립트 재실행 → 커밋(신규 주소만 지오코딩).

## 3. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 지도 | **V-World WMTS 타일 + Leaflet** | V-World 공식 타일(`api.vworld.kr/req/wmts/1.0.0/{KEY}/Base/{z}/{y}/{x}.png`)을 Leaflet에서 사용. 경량(40KB)이라 모바일에서 빠르고, 마커 클러스터링 플러그인(`leaflet.markercluster`) 활용 가능 |
| 프런트엔드 | **바닐라 JS + HTML/CSS (빌드 도구 없이 정적 사이트)** | 데이터 189건·화면 3개 수준의 규모에는 프레임워크 불필요. GitHub Pages에 그대로 배포 가능 |
| 데이터 | 정적 `stores.json` (약 60KB) | 서버·DB 불필요, 첫 로딩 시 1회 fetch |
| 변환 스크립트 | Python (`openpyxl` + `requests`) | 엑셀 파싱 + V-World 지오코딩 |
| 배포 | **GitHub Pages** | 무료, 저장소와 일체형. V-World API 키에 배포 도메인(`*.github.io`) 등록 필요 |

> **사전 준비물**: [vworld.kr](https://www.vworld.kr) 개발자 등록 후 API 키 발급, 키에 서비스 도메인 등록(로컬 개발용 `localhost` 포함).

## 4. UX 흐름 (2단계 카테고리 선택)

```
[초기 화면]
  파주시 전체 지도 + 권역별 업소 수 요약
      │
      ▼  ① 권역 선택 (12개: 문산 38 · 법원 37 · 탄현 26 …)
[권역 화면]
  지도가 해당 권역으로 이동/확대, 권역 내 전체 마커 표시
      │
      ▼  ② 업종 카테고리 선택 (전체 / 음식점 / 카페·간식 / 미용 / 목욕 / 숙박)
[필터 결과]
  마커 + 업소 목록 갱신
      │
      ▼  ③ 마커 or 목록 항목 탭
[업소 상세]
  업소명 · 업종 · 할인현황 · 할인세부내역 · 주소 · 사회복무요원 여부
  [📞 전화 걸기] [🧭 길찾기(카카오/네이버맵 딥링크)]
```

- 권역·업종 선택 상태는 URL 해시(`#region=문산&cat=food`)에 반영 → 뒤로가기·공유 지원
- 보조 기능: 업소명 검색창, "사회복무요원 가능" 토글, 현재 위치 버튼(Geolocation)

## 5. 반응형 화면 설계

### 모바일 (< 768px) — 기본
```
┌──────────────────────┐
│ 헤더: 앱 제목 · 검색 🔍 │
│ 권역 칩 가로 스크롤     │  ← [전체][문산 38][법원 37][탄현 26]…
│ 업종 칩 가로 스크롤     │  ← [전체][음식점][카페][미용][목욕][숙박]
│                      │
│      V-World 지도     │  (남은 화면 전체)
│        + 마커         │
│                      │
├──────────────────────┤
│ ▲ 바텀시트 (목록 N개)  │  ← 끌어올리면 업소 리스트, 탭하면 상세
└──────────────────────┘
```

### PC (≥ 768px)
```
┌────────────┬──────────────────────┐
│  사이드바    │                      │
│  권역 선택   │      V-World 지도     │
│  업종 선택   │        + 마커         │
│  업소 목록   │                      │
│  (스크롤)   │   [상세 팝업/패널]      │
└────────────┴──────────────────────┘
```

- CSS Grid + 미디어쿼리 1개(768px)로 전환. 지도는 `resize` 시 `map.invalidateSize()` 호출
- 터치 타깃 44px 이상, 칩 UI는 `overflow-x: auto` 스냅 스크롤
- 다크모드는 시스템 설정(`prefers-color-scheme`) 따름 (선택 사항)

## 6. 파일 구조 (목표)

```
paju-military-discount-map/
├── index.html
├── css/style.css
├── js/
│   ├── app.js          # 초기화, 상태 관리(권역/업종/검색), URL 해시 동기화
│   ├── map.js          # Leaflet + V-World 타일, 마커/클러스터 관리
│   └── ui.js           # 칩, 바텀시트, 목록, 상세 패널 렌더링
├── data/stores.json    # 변환된 업소 데이터 (좌표 포함)
├── scripts/
│   └── convert.py      # 엑셀 → JSON 변환 + V-World 지오코딩
└── ★2026년도 군 장병 할인업소 현황(...).xlsx   # 원본 (보존)
```

`stores.json` 레코드 예시:
```json
{
  "id": 4, "region": "광탄", "category": "food",
  "name": "신나는 등갈비 엔 닭발", "tel": "031-943-3545",
  "addr": "경기도 파주시 광탄면 혜음로 1117-1, 1층",
  "discount": "10% 할인", "detail": "10% 할인",
  "socialWorker": true, "lat": 37.79, "lng": 126.86
}
```

## 7. 개발 단계

| 단계 | 작업 | 산출물 |
|---|---|---|
| **1. 데이터 파이프라인** | 변환 스크립트 작성, V-World 지오코딩, 실패 건 수동 보정 | `scripts/convert.py`, `data/stores.json` |
| **2. 지도 기본 화면** | Leaflet + V-World 타일, 전체 마커 + 클러스터, 마커 팝업 | `index.html`, `map.js` |
| **3. 필터 & 목록** | 권역 칩 → 업종 칩 2단계 필터, 업소 목록/상세, 전화·길찾기 링크, 검색, 사회복무요원 토글, URL 해시 | `app.js`, `ui.js` |
| **4. 반응형 & 마감** | 모바일 바텀시트 / PC 사이드바 전환, 접근성(터치 타깃·명도 대비), 성능 점검 | `style.css` |
| **5. 배포** | GitHub Pages 활성화, V-World 키 도메인 등록, 실기기(iOS/Android) 확인 | 서비스 URL |

예상 규모: 정적 페이지 1장 + JS 3개 파일 수준. 1~5단계를 순서대로 진행하며 단계별로 커밋한다.

## 8. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 지오코딩 실패 주소 (동·호수 표기 등) | 스크립트가 실패 목록 출력 → 주소 정제 후 재시도, 최후엔 수동 좌표 입력 |
| V-World API 키 도메인 제한 | 배포 도메인 확정 후 키에 등록. 타일은 키 없는 대체(브이월드 장애 시 OSM 폴백) 고려 |
| V-World 일일 트래픽 제한 | 타일 요청만 발생(지오코딩은 빌드 타임 1회)이라 소규모 서비스엔 충분 |
| 데이터 갱신(연 1~2회) | 엑셀 교체 → `convert.py` 재실행만으로 반영되는 구조 유지 |
